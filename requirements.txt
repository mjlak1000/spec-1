# SPEC-1 — v0.1 dependencies
# Core loop has zero external dependencies by design.
# Add real dependencies here as you wire in live sources.

# Dev / testing
pytest>=8.0
pytest-cov>=5.0

# Future: uncomment as you add real adapters
# httpx>=0.27          # HTTP client for source fetching
# feedparser>=6.0      # RSS feed parsing
# anthropic>=0.25      # Claude API for signal analysis
# fastapi>=0.110       # API layer
# sqlalchemy>=2.0      # PostgreSQL ORM
# psycopg2-binary>=2.9 # PostgreSQL driver
# apscheduler>=3.10    # Scheduler
